# GoShield - Environment Variables

# The base URL of the local API for form submissions.
# Nuxt automatically prefix variables starting with NUXT_PUBLIC_ to make them available client-side.
NUXT_PUBLIC_API_BASE_URL=http://localhost:3000
